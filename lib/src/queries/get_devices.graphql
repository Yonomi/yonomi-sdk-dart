# import "./device_details.fragment.graphql"

query getDevices {
    me {
        devices {
            pageInfo { hasNextPage }
            edges {
                node {
                    ... DeviceDetails
                }
            }
        }
    }
}
