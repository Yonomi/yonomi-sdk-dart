mutation makeInstallLatestFirmwareUpdateRequest($deviceId: ID!) {
  scheduleLatestFirmwareUpdate(deviceId: $deviceId) {
    actionId
    transactionId
    device {
      traits {
        name
        instance
      }
    }
  }
}
